{
  "name": "coffin",
  "version": "0.0.1",
  "description": "Sandbox module for node",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "npm run test -- --watch",
    "test:coverage": "npm run test -- --coverage"
  },
  "author": "Krisztian Nagy",
  "license": "MIT",
  "devDependencies": {
    "jest": "^18.1.0",
    "sandbox": "^0.8.6"
  },
  "jest": {
    "modulePathIgnorePatterns": [
      "<rootDir>/data/"
    ]
  }
}
